import { NextPage } from 'next';
// containers
import PreReleaseLandingContainer from '../page-containers/PreReleaseLandingPage';

const PreRegisteration: NextPage = () => {
  return <PreReleaseLandingContainer />;
};
export default PreRegisteration;
